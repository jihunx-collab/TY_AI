# 팀 공용 스킬 폴더

여기에 만든 스킬은 이 레포에서 `claude` 를 실행하면 **자동으로 인식**된다.
커밋해서 push 하면 팀 전원이 `git pull` 로 같은 스킬을 쓴다.

만드는 법 → [../../docs/02-skill-guide.md](../../docs/02-skill-guide.md)

```
.claude/skills/
└── 스킬이름/
    ├── SKILL.md        ← 필수. 절차와 규칙
    └── references/     ← 선택. 보고서 양식, 체크리스트 등
```
